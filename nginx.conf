events {}

http {
  include mime.types;

  server {
    listen 80;

    location /api/ {
      proxy_pass http://straribay_back:4000/;
    }

    location / {
      root /app/build;
      try_files $uri /index.html;
    }
  }
}